import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.travicons',
  appName: 'Travicons',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
